
[English](./README.md)   | [Russian](./README_RU.md)
# 🤖Gemini Chatbot (Go/REST API)

  Простой и надежный консольный чат-бот, разработанный на Go (Golang) для взаимодействия с языковой моделью Google Gemini 2.5 Flash посредством прямого вызова REST API. Проект демонстрирует базовые навыки интеграции с внешними AI-сервисами и правильной обработки сетевых ошибок.

## ⚙️Технологический стек и инструменты


-   Язык программирования: Go (Golang)

-  API: Google Gemini API (`gemini-2.5-flash model`)

-   Метод связи: прямые HTTP-запросы (REST)

-   Формат данных: JSON

-   Используемые пакеты Go: `net/http`, `encoding/json`, `os`, `log`

-  Среда: консольное (CLI) приложение (UPD. добавлен веб-интерфейс)


# 🔑 Ключевые особенности и характеристики

1. Стабильность и обработка ошибок
- Надежная обработка ошибок API: включены проверки статуса HTTP (`200 OK`, `404`, `503` и т. д.), обеспечивающие правильное завершение программы в случае проблем с сервером или ключом API.
- Защита от паники: перед доступом к индексу `[0]` выполняется проверка длины массива (`Candidates`, `Parts`), что полностью исключает ошибку `panic error: runtime error: index out of range`.
- Диагностика: разбор и вывод информации из поля `PromptFeedback` для диагностики причин, по которым ответ может быть заблокирован (например, по соображениям безопасности).

2. Архитектура и взаимодействие
- Прямая интеграция REST: использование стандартного пакета Go `net/http` для независимого формирования и отправки запросов JSON, минуя официальные SDK.
- Структурированный ввод/вывод: запросы пользователей принимаются через аргументы командной строки (`os.Args`). Ответ модели извлекается из сложной структуры JSON и отображается в виде простого текста.
- Безопасность ключей: ключ API загружается из переменной среды (`GEMINI_API_KEY`), что предотвращает его случайную публикацию в исходном коде.


# 💻 Интерфейс

При размещении на хостинге он генерирует современный и привлекательный HTML-интерфейс в окне браузера.
